[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "talentfit"
version = "0.1.0"
description = "Potential Talents: candidate ranking and re-ranking with feedback."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
packages = ["talentfit"]
